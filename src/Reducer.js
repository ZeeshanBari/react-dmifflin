const reducer = (state, action) => {
	switch (action) {
		// Items
		case 'add_item':
			return;
		case 'remove_item':
			return;
		case 'edit_item':
			return;

		// Categories

		default:
			return;
	}
}

export default reducer;